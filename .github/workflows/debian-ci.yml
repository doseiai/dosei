name: debian-ci

on:
  push:
    branches: [ main ]
    tags:
      - '*'
  pull_request:
    branches: [ main ]

concurrency:
  group: ${{ github.ref }}-deb
  cancel-in-progress: true

jobs:
  ci:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust
        uses: dsherret/rust-toolchain-file@v1

      - name: Rust Cache
        uses: Swatinem/rust-cache@v2
        with:
          cache-all-crates: true

      - name: Install Protoc
        uses: arduino/setup-protoc@v2
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Install cargo-deb
        run: cargo install cargo-deb

      - name: Setup deb-s3
        uses: alw3ys/setup-deb-s3@v1

      - name: Cargo deb
        run: cargo deb --package doseid


      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: AKIASZDFCITSGESB2DSD
          aws-region: eu-west-1
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

      - name: Publish to S3
        working-directory: target/debian
        run: deb-s3 upload --bucket packages.dosei.io doseid_0.0.17-1_amd64.deb
