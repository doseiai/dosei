[package]
name = "dosei_util"
version = { workspace = true }
edition = { workspace = true }

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
toml = { workspace = true }
regex = { workspace = true }

pyo3 = "0.20.0"
walkdir = "2.4.0"

[dev-dependencies]
tempfile = "3.9.0"
